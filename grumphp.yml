parameters:
  git_dir: .
  bin_dir: vendor/bin
  tasks:
    phpcs:
      standard: PSR2
      ignore_patterns:
        - resources/
        - database/
        - bootstrap/
        - tests/
    phpunit:
